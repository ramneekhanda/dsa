fns:
  - &gallery_fn |
      // Static content, redrawn every tick - see this chapter's "Try it" note
      // on why this can't just be a one-time on_init() draw() call.
      fn on_timer() {
        draw([
          #{ shape: "rect", x: -70, y: 50, w: 40, h: 24, fill: "#8ecae6", stroke: "#000000", stroke_width: 1 },
          #{ shape: "roundedrect", x: -20, y: 50, w: 40, h: 24, fill: "#8ecae6", stroke: "#000000", stroke_width: 1 },
          #{ shape: "circle", x: 40, y: 50, r: 14, fill: "#ffb703", stroke: "#000000", stroke_width: 1 },
          #{ shape: "line", x1: -90, y1: 10, x2: 90, y2: 10, stroke: "#333333", stroke_width: 2 },
          #{ shape: "polygon", points: [[-20, -20], [20, -20], [0, 20]], closed: true, fill: "#e76f51" },
          #{ shape: "text", x: 0, y: -50, text: "rect / roundedrect / circle / line / polygon / text", size: 9, color: "#333333" }
        ]);
      }

graph_defn:
  graph:
    - name: gallery
      node_type: gallery
      links: []
  graph_attrs:
    title: Shape Primitives
  node_types:
    - id: gallery
      fn: *gallery_fn
      attrs:
        ticks: 2
